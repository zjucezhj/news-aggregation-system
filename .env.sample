# =============================================================================
# 新闻聚合系统配置文件模板
# 请复制此文件为 .env 并填入您的实际配置
# =============================================================================

# SMTP 邮件服务器配置
# 用于发送新闻摘要邮件
SMTP_HOST=smtp.example.com                    # SMTP服务器地址
SMTP_PORT=465                                 # SMTP端口
SMTP_USERNAME=your_email@example.com          # SMTP用户名
SMTP_PASSWORD=your_password                  # SMTP密码
SMTP_USE_TLS=True                             # 是否使用TLS加密

# 邮件发送设置
SENDER_EMAIL=your_email@example.com           # 发件人邮箱
RECIPIENT_EMAIL=admin@example.com              # 默认收件人邮箱（可选）

# 订阅者配置
# 格式说明：
# - id: 订阅者唯一标识符
# - email: 接收邮件的邮箱地址
# - keywords: 关键词列表，系统会匹配包含这些关键词的文章
# - NEWS_SOURCES: 新闻源列表，系统会从这些网站抓取文章
SUBSCRIBERS=[
  {
    "id": "1",
    "email": "subscriber1@example.com",
    "keywords": ["technology", "AI", "innovation"],
    "NEWS_SOURCES": ["https://techcrunch.com", "https://www.theverge.com"]
  },
  {
    "id": "2", 
    "email": "subscriber2@example.com",
    "keywords": ["business", "finance", "economy"],
    "NEWS_SOURCES": ["https://www.bloomberg.com", "https://www.reuters.com"]
  }
]

# 爬取模式配置
# incremental: 增量模式，只爬取新文章（推荐日常使用）
# full: 全量模式，重新爬取所有文章（首次使用或数据重置时）
CRAWL_MODE=incremental

# 系统设置（可选）
# MAX_ARTICLES_PER_SOURCE=50                     # 每个新闻源最多爬取文章数
# ENABLE_EMAIL_NOTIFICATIONS=true               # 是否启用邮件通知
# CONTENT_PREVIEW_LENGTH=500                    # 内容预览长度
# KEYWORD_MATCH_TYPE=whole_word                 # 关键词匹配类型: whole_word 或 partial
# CASE_SENSITIVE_MATCHING=false                 # 是否区分大小写匹配
# ENABLE_DEBUG_MODE=false                       # 是否启用调试模式
# OUTPUT_FILE_PREFIX=news_aggregation            # 输出文件前缀